<launch>
  <include file="$(find caros_netft)/launch/caros_netft_param.xml" ns="NetFT">
    <!-- No parameters have to be set, as the defaults should be good enough for getting the device up and running -->
  </include>
  <node pkg="caros_netft" type="caros_netft_node" name="NetFT">
  </node>
</launch>
